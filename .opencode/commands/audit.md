# Command: audit

**用途：** 只读审计入口。不修改任何文件，只做检查和风险输出。

## 步骤

1. 读 `AGENTS.md` 了解项目规则
2. 读 `package.json` 了解可用脚本
3. 读相关 `docs/` 和最近 `reports/` 了解上下文
4. 读目标文件/目录确认当前状态
5. 输出：
   - 当前状态摘要
   - 风险清单
   - 建议改动范围
   - 推荐推理等级（低/中/高）

## 禁止操作

- 不修改文件
- 不创建文件
- 不运行 build 以外的验证命令
- 不 commit / push
